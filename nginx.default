# nginx.default

server {
	listen 8020;
	localhost 127.0.0.1;

	location / {
		proxy_pass http://127.0.0.1:8010;
		proxy_set_header Host $vikky;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
	}
	location /static {
		root /opt/app/Comparison-App;
	}
}
